aiogram==3.30.0
# Imported directly by services/geo_utils.py and services/samsara_service.py
# (async HTTP for geocoding/Samsara) - pinned explicitly rather than relying
# on aiogram's own transitive pin, so an aiogram upgrade that narrows/drops
# it can't silently break GPS polling or geocoding.
aiohttp==3.14.3
google-genai==1.3.0
google-auth==2.56.3
google-auth-oauthlib==1.2.1
google-api-python-client==2.154.0
python-dotenv==1.2.3
cryptography==50.0.0
sqlalchemy==2.0.36
fastapi==0.141.1
uvicorn==0.34.0
pyjwt==2.13.0
bcrypt==4.2.1
email-validator==2.3.0
slowapi==0.1.10
requests==2.34.2
stripe==11.4.1

# Two-factor authentication
pyotp==2.9.0
qrcode[pil]==8.0
webauthn==2.5.2
# Required by webauthn (not imported directly here) - pinned explicitly
# since it's a security-relevant transitive dependency.
pyopenssl==26.4.0
# Optional - only needed if SMS_PROVIDER=twilio in .env:
# twilio==9.4.1
